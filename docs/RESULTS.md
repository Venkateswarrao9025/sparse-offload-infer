# Results

Not yet populated. This file will report every headline number from the
project, each traceable to a CSV in `reports/` (PROJECT_SPEC.md sec 6, M9
acceptance). No number goes in here unless it's backed by a checked-in CSV.

Pending: `reports/m0_pcie_bandwidth.csv`, `reports/m0_baseline.csv`.
